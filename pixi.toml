[workspace]
channels = ["conda-forge", "bioconda", "r"]
name = "SAIGEQTL"
platforms = ["linux-64", "linux-aarch64", "osx-64", "osx-arm64"]
version = "0.3.6.0"

[tasks]
install-github-deps = "Rscript -e 'remotes::install_github(\"barkasn/fastSave\")'"
install-standard = { depends-on = ["install-github-deps"], cmd = "bash -c 'LIB_PATH=${SAIGEQTL_LIB_PATH:-$(R -q --slave -e \"cat(.libPaths()[1])\")}; R CMD INSTALL . --library=\"$LIB_PATH\" --no-lock --preclean'" }

#build = "R CMD INSTALL ."
#test = "Rscript -e 'library(SAIGEQTL); cat(\"SAIGE-QTL loaded successfully\\n\")'"

[dependencies]
# System dependencies
boost-cpp = "*"
cget = "*"
openblas = "*"
zlib = "*"
zstd = "*"
savvy = "*"
superlu = "*"
zlib-ng = "*"
nomkl = "*"
llvm-openmp = "*"
tbb = "*"
tbb-devel = "*"
cxx-compiler = "*"
make = "*"
libblas = { version = "*", build = "*openblas" }
liblapack = { version = "*", build = "*openblas" }

# R and core packages
r-base = ">=4.0"
r-devtools = "*"

# Core R computational packages
r-rcpp = "*"
r-rcpparmadillo = "*"
r-rcppeigen = "*"
r-rcppparallel = "*"
r-rcppnumerical = "*"
r-bh = "*"
r-cpp11 = "*"

# Data manipulation and I/O
"r-data.table" = "*"
r-dplyr = "*"
r-dbplyr = "*"
r-matrix = "*"
r-qlcmatrix = "*"
r-furrr = "*"

# Standard R packages
r-nlme = "*"
r-mass = "*"

# Statistical packages
r-skat = "*"
r-metaskat = "*"
r-spatest = "*"

# Utility packages
r-optparse = "*"
r-rhpcblasctl = "*"
r-rsqlite = "*"
r-remotes = "*"
htslib = ">=1.22.1,<2"

# Note: one package not available in conda channels:
# - fastSave (GitHub: barkasn/fastSave)
# These are handled via the install-github-deps task
